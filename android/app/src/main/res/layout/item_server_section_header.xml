<?xml version="1.0" encoding="utf-8"?>
<!--
    Section header for the server list RecyclerView.
    Displays section title ("Saved Servers", "Nearby Servers") with optional scanning indicator.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="8dp">

    <!-- Section Title -->
    <TextView
        android:id="@+id/sectionTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceTitleSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="12sp"
        android:textAllCaps="true"
        android:letterSpacing="0.1"
        tools:text="SAVED SERVERS"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Scanning Indicator (shown on "Nearby Servers" when mDNS is active) -->
    <ProgressBar
        android:id="@+id/scanningIndicator"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginStart="8dp"
        android:visibility="gone"
        android:indeterminate="true"
        tools:visibility="visible"
        app:layout_constraintStart_toEndOf="@id/sectionTitle"
        app:layout_constraintTop_toTopOf="@id/sectionTitle"
        app:layout_constraintBottom_toBottomOf="@id/sectionTitle" />

    <!-- Scanning Text (shows "Scanning..." next to spinner) -->
    <TextView
        android:id="@+id/scanningText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:text="@string/scanning_ellipsis"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="12sp"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintStart_toEndOf="@id/scanningIndicator"
        app:layout_constraintTop_toTopOf="@id/sectionTitle"
        app:layout_constraintBottom_toBottomOf="@id/sectionTitle" />

    <!-- Empty Section Hint (shown when section has no items) -->
    <TextView
        android:id="@+id/emptyHint"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:alpha="0.7"
        android:visibility="gone"
        tools:text="Add a server using the + button"
        tools:visibility="visible"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/sectionTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>
